This directory contains reference files used by 'grand'.

The environment variable GRAND_REFDIR points at the directory containing
reference files for 'grand', usually this directory 'fref'

Use the following commend to see the references files used by 'grand.F'.
grandsol/fort% grep -A 1 "= trim(REFDIR) //" grand.F
               FILENAME255 = trim(REFDIR) // 
     .                       '/BSTAR_CORRECTION.2012.10.23'
--
         FILENAME255 = trim(REFDIR) // 
     .                '/nsosun.tem'
--
         FILENAME255 = trim(REFDIR) // 
     .                '/kecknso52p5.sam'
[[Output last updated on 2016-Jan-25]]

Thus, 'grand' expects the following three files to exist in 'fref':
  BSTAR_CORRECTION.2012.10.23
  nsosun.tem
  kecknso52p5.sam
[[Reference file list last updated on 2016-Jan-25]]
